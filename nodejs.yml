version: "3"
services:
  nodejs:
    image: node-server:latest
    networks:
      - nodejs
    deploy:
      replicas: 1
      update_config:
        parallelism: 2
        delay: 10s
      restart_policy:
        condition: on-failure

networks:
  nodejs:

volumes:
  db-data:
