version: "3.3"
services:
  traefik:
    image: traefik
    command: --web \
      --docker \
      --docker.swarmmode \
      --docker.domain=vicoders.com \
      --docker.watch \
      --logLevel=DEBUG
    networks:
      - web-net
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /dev/null:/traefik.toml
    deploy:
      placement:
        constraints: [node.role==manager]
 
  portainer:
    image: portainer/portainer
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "portainerdata:/data"
    networks:
      - web-net
    deploy:
      placement:
        constraints: [node.role == worker]
      labels:
        - "traefik.enable=true"
        - "traefik.port=9000"
        - "traefik.docker.network=web-net"
        - "traefik.frontend.rule=Host:portainer.vicoders.com"
 
  ms1:
    image: melvindave/spring-boot-example
    networks:
      - traefik-net
    depends_on:
      - mongodb
    deploy:
      replicas: 1
      placement:
        constraints: [node.role == worker]
      labels:
        - "traefik.enable=true"
        - "traefik.port=8080"
        - "traefik.docker.network=web-net"
        - "traefik.frontend.rule=Host:ms1.vicoders.com"

  node1:
    image: localhost:5000/mii:latest
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.port=3000"
        - "traefik.docker.network=web-net"
        - "traefik.frontend.rule=Host:node1.vicoders.com"
      restart_policy:
        condition: any
      mode: replicated
      replicas: 1
      placement:
        constraints: [node.role == worker]
      update_config:
        delay: 2s
      restart_policy:
        condition: on-failure

  mongodb:
    image: mongo
    volumes:
      - "mongodata:/data/db"
    networks:
      - web-net
    deploy:
      replicas: 1
    ports:
      - "27017:27017"  
 
networks:
  web-net:
 
volumes:
   portainerdata:
   mongodata: